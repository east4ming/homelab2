apiVersion: v1
kind: ConfigMap
metadata:
  name: smartctl-exporter-dashboard
  namespace: {{ .Release.Namespace }}
  labels:
    grafana_dashboard: "1"
    grafana_dashboard_folder: "SMART"
data:
  smartctl-exporter-dashboard.json: |-
{{ .Files.Get "files/dashboards/smartctl-exporter-dashboard.json" | indent 4 }}
