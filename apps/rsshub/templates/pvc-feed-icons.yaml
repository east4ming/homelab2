apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  name: feed-icons
  namespace: rsshub
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Mi
